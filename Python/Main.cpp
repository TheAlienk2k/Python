#include <iostream> 
#include <SFML/Graphics.hpp>
using namespace std;

int main()
{
    sf::RenderWindow gameWindow(sf::VideoMode({ 1400, 650 }), "Python The Game", sf::Style::Titlebar | sf::Style::Close);

    while (gameWindow.isOpen())
    {
        



    }
    
}